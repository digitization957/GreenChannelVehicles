# Taskdown

- Added Buyer.aspx (gate entry form: vehicle no, transporter, Without PCS / Manual PCS toggle logic, 50-char material field, submit confirmation modal, success receipt) with Buyer.aspx.cs WebMethod `SubmitVehicle`.
- Added Security.aspx (live card dashboard, auto-refresh polling, "Vehicle is inside gate" action) with Security.aspx.cs WebMethods `GetVehicles` / `MarkVehicleInside`.
- Added VehicleGateStore.cs: thread-safe in-memory store with server-side validation (regex on vehicle no/transporter, 50-char + unsafe-char check on material). No DB used yet — data resets on app pool recycle.
- Added Content/gcv.css: Hallmark-styled shared token system (Mahindra red accent, OKLCH tokens, 4pt spacing, full interactive states) used by both pages.
- Added Scripts/buyer.js and Scripts/security.js: client validation, AJAX calls to WebMethods, safe DOM rendering (`.text()`, no `innerHTML` of user data) to avoid stored XSS.
- Security hardening: added CSP-adjacent headers (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, X-XSS-Protection) and disabled ASP.NET version header in Web.config; server-side regex validation blocks HTML/script injection in free-text fields.
- Wired Buyer.aspx / Security.aspx into GreenChannelVehicles.csproj and linked from Default.aspx home page.
- Redesigned Security cards: removed the colored left accent border (AI-slop pattern), replaced badges with a quieter status-tag (pulsing dot for Pending, checkmark for Inside, muted card background once inside).
- Security dashboard is now live: polling dropped to 3s, newly-arrived vehicles get a highlight glow + toast ("New vehicle · <plate>") instead of silently appearing.
- Added a search box (top-right of Security toolbar) filtering by vehicle no / transporter / material, with match highlighting and a dedicated "no matches" empty state.
- Buyer form: Vehicle no / Transporter / Material are the only mandatory fields (marked with *); Without PCS / Manual PCS are now optional and shown side by side in a two-column row.
- Backend (VehicleGateStore, Buyer.aspx.cs) updated: WithoutPCS is now nullable and no longer enforced server-side, matching the relaxed validation.
- Added App_Data/schema.sql: MySQL schema (gcv_db.vehicle_entries table matching VehicleEntry fields) + least-privilege app user. Not wired into the app yet — DB access wasn't available in this environment, app still runs on the in-memory VehicleGateStore.
- Built the app (no Visual Studio/MSBuild web targets installed) and ran it via IIS Express on http://localhost:8080 — Default/Buyer/Security all return 200.
- Fixed Security dashboard toolbar: stat chips (Live/Pending/Inside) + search bar now stay right-aligned instead of drifting to center when wrapping on mid-width screens.
- Security dashboard polling made sophisticated: 10s interval (was 3s), cards now diff/patch in place instead of full re-render (no flicker, preserves DOM state), and polling pauses entirely while a "mark inside" call is in flight, resuming with an instant resync right after. True WebSockets would need SignalR (new server dependency, out of current html/js/webmethod stack) — this gets the same live feel via smart pausing + in-place patching.
- Added Buyer Name and Token as new required fields: Buyer.aspx form + buyer.js validation/receipt, VehicleGateStore (validated, stored), Buyer.aspx.cs SubmitVehicle, Security.aspx card + security.js (display, patch, search), and App_Data/schema.sql (buyer_name, token columns). Rebuilt and restarted the app on http://localhost:8080 — verified via a live SubmitVehicle/GetVehicles round-trip.
- Moved Buyer name and Token to the top of the Buyer form, side by side in a two-column row (new .field-row grid class, reused from pcs-row).
- Split the Buyer form into two labeled sections (Buyer information / Vehicle information) with a divider, so buyer identity fields are visually separate from vehicle/material fields.
- Scaled the whole UI down for 720p target monitors: root font-size drops 16px->14px under max-height:800px, which proportionally shrinks every rem-based spacing/type/radius token (the whole design system) so it doesn't look oversized compared to the 1080p dev display.
- Wired the app to Azure MySQL: added connectionStrings (GcvDb, host mazpngpappmysql01.mysql.database.azure.com, db gcv_db, Uid ngpdbadm, Pwd left blank on purpose - never hardcode secrets, set it on the testing/prod box only). Installed MySql.Data 8.0.33 + deps via nuget.exe (no internet/DB access needed beyond nuget.org), rewrote VehicleGateStore.cs to run parameterized SQL (INSERT/UPDATE/SELECT) against vehicle_entries instead of the in-memory dictionary - same public API, so Buyer.aspx.cs/Security.aspx.cs needed no changes. Added binding redirects for the new assemblies to avoid version-mismatch load failures. Not testable here - this dev PC can't reach the Azure private-link DB; verify on the testing PC once the real password is set.
- Built the Gate Watch kiosk app in new /kiosk folder: Python + pywebview fullscreen no-interaction window, a WebSocket server (server.py) that polls MySQL vehicle_entries (WHERE is_inside = 0) every 3s and pushes changes, and ui/index.html (light board, Mahindra-red accent, HSRP-style plate for vehicle no with "MH 12 XY5566" spacing, real Green Channel + Mahindra logos, bigger Transporter/Material type). Design approved via Artifact iteration first. requirements.txt + smoke-tested locally (installs clean, WS server binds/listens, static file server serves all assets 200) — DB itself unreachable from this dev PC same as the main app, so live data untested here.
- Main app changes: id column switched to MySQL AUTO_INCREMENT (dropped the app-generated GUID, VehicleGateStore now reads back LastInsertedId); Security dashboard (GetAll) now filters to today's date only; added a slight vertical separator between the Yes/No toggle options; Material details is now optional (server + client); added a required PG dropdown (Buyer info section) populated via new GetPGList WebMethod from a separate plant_master.tbl_PG database (WHERE Plant_ID = 4, shows PG_Name, saves PG_ID as pg_id) - added PlantMasterDb connection string to Web.config (assumed same server/creds as GcvDb, different schema - correct if wrong). schema.sql updated (id auto-increment, pg_id column, material nullable). Verified both pages at a 720p viewport via headless Edge screenshots - scaling holds, nothing oversized. plant_master itself is unreachable from this dev PC same as gcv_db, so GetPGList/live PG data untested here.
- Security cards made more compact for 720p: tighter card padding/gaps, smaller grid minmax (300px->230px), so more cards fit and the card-to-text ratio feels right instead of big-card/small-text. Vehicle number on Security cards now uses the same HSRP-style plate badge as the kiosk (yellow, black border, blue IND strip, "MH 12 XY5566" spacing). Replaced the red "GC" text-badge logo with the real gclogo.webp on both Buyer and Security topbars - also had to add an IIS .webp MIME type mapping to Web.config (IIS Express was 404.3'ing it, unregistered extension). Verified via screenshots at 1280x720 + curl checks; couldn't verify populated Security cards visually since the DB is unreachable from this dev PC.
- Kiosk fixes: fixed CSS bug hiding real logo sizing (.bd-logo img selector never matched, class was on the img itself), added .webp MIME mapping to the kiosk's Python static server, added CURDATE() filter so kiosk only shows today's vehicles, added left-aligned pending-count in footer. Confirmed latest-first order, auto-remove on mark-inside, and pending-only filter were already correct. Verified with mock data screenshot at 1280x720.
